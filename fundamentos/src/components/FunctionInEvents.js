// Criação de uma função em um evento.

const FunctionInEvents = () => {

    return (
        <div>
            <div>
                <button onClick={() => console.log("Clicado com sucesso!")}>Clique aqui também!</button>
            </div>
        </div>
    );
};

export default FunctionInEvents;
